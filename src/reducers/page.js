const initialState = {
  repository: {
    name: "github_client",
    description: "React practice project in ITIS KFU",
    status: "public"
  }
};

export function pageReducer(state = initialState) {
  return state;
}